# نایلونیوم — گزارش کامل پروژه

سند تحویل. هرچه در طول ساخت این سایت تصمیم گرفته شد، ساخته شد، خراب شد و درست شد،
از ابتدا تا امروز.

- **سایت زنده:** https://iconiccerberrus.github.io/Nylonium/
- **مخزن:** https://github.com/IconicCerberrus/Nylonium
- **آخرین وضعیت:** ۱۴۷ صفحه منتشرشده، ۱۹ کامیت، بدون کار ناتمام

---

## ۱. صورت مسئله

سایت معرفی محصولات شرکت **نایلونیوم**، تولیدکننده انواع نایلون صنعتی، کشاورزی و
بسته‌بندی.

**نکته کلیدی مدل کسب‌وکار:** سایت فروشگاهی نیست. هیچ خرید و پرداختی انجام نمی‌شود.
هدف این است که بازدیدکننده محصولات را ببیند و برای معامله به تلگرام هدایت شود.
این یک تصمیم معماری است، نه فقط یک ویژگی — تقریباً همه‌ی تصمیم‌های بعدی از همین
یک جمله بیرون آمدند.

**الهام‌گرفته از:** [chabokplast.com](https://chabokplast.com) — با هدف مدرن‌تر،
منظم‌تر و سریع‌تر بودن.

### تصمیم‌های اولیه (پرسیده و تأیید شده)

| موضوع | انتخاب |
|---|---|
| زبان و جهت | فارسی، RTL کامل، فونت Vazirmatn لوکال |
| پالت رنگ | سبز زمردی + طوسی سرد، لایت و دارک هر دو |
| استایل | مدرن، گلس/گرادیان، انیمیشن نرم |
| کانال‌های تماس | تلگرام، واتساپ، تماس تلفنی (بدون اینستاگرام) |
| نام شرکت | نایلونیوم |
| تصاویر محصول | ندارد — بعداً اضافه می‌شود |
| شماره تماس | `+98 994 430 9705` |

---

## ۲. پشته فنی

- **React 19** + **Vite 8** — ساخته‌شده به‌صورت چندصفحه‌ای (MPA)
- **Tailwind CSS v4** — کل پیکربندی داخل `src/index.css`، بدون `tailwind.config`
- **lucide-react** برای آیکون‌ها
- **Vazirmatn Variable** لوکال از `@fontsource-variable` — بدون درخواست به CDN
- بدون بک‌اند، بدون API، بدون دیتابیس
- انتشار روی GitHub Pages با GitHub Actions

**framer-motion نصب شد و بعد حذف شد** — همان انیمیشن‌ها با CSS سبک‌تر درآمدند.

---

## ۳. سیر ساخت

### مرحله ۱ — صفحه اول (`613048c`)

یک صفحه، سیزده بخش: نوبار، هیرو، نوار متحرک محصولات، آمار، محصولات، چرا نایلونیوم،
کاربردها، فرآیند سفارش، نظر مشتریان، سؤالات متداول، بنر CTA، تماس، فوتر.

نکته‌های این مرحله:

- **کنتراست اندازه‌گیری شد، نه حدس زده شد.** سبز `brand-600` روی سفید فقط ۳٫۶۶:۱
  می‌داد که زیر کف ۴٫۵:۱ استاندارد WCAG AA برای متن کوچک است. توکن `--brand-ink`
  ساخته شد که در تم لایت به `brand-700` (۵٫۳۷:۱) و در دارک به `brand-300`
  (۱۳٫۲۶:۱) می‌رسد. **همیشه برای متن سبز از این توکن استفاده کن، نه از
  `brand-500/600` مستقیم.**
- ده SVG اختصاصی برای ده محصول، به‌جای عکس.
- ریسپانسیو از ۳۲۰px تا دسکتاپ عریض، بدون اسکرول افقی.

### مرحله ۲ — انتشار روی GitHub Pages (`98c9ce7`)

مسیر پرپیچ‌وخمی داشت که ارزش ثبت دارد:

1. اولین push شکست خورد — اسم کاربری `IconicCerberrus` است با **دو تا R**، و من از
   روی اسکرین‌شات `IconicCerberus` خوانده بودم.
2. ریپو به `Nylonium.io.github` تغییر نام داده شده بود و روی Pages یک دامنه‌ی
   اختصاصی `nylonium.io.github` ست شده بود که دامنه‌ی واقعی نیست. برگردانده شد به
   `Nylonium` و CNAME حذف شد.
3. صفحه سیاه نشان می‌داد چون Pages فایل‌های خام مخزن را سرو می‌کرد. با ورک‌فلوی
   Actions که `dist/` را منتشر می‌کند حل شد.

### مرحله ۳ — همه چیز به پاپ‌آپ (`5e66b0d`)

هر کارتی که قبلاً مستقیم به بیرون لینک می‌داد، حالا اول یک پاپ‌آپ باز می‌کند.
دلیلش صریح بود: کاربر ممکن است فقط کنجکاو باشد و نخواهد یکهو تماس گرفته شود، و
ممکن است چند شماره داشته باشیم که باید بین‌شان انتخاب کند.

سه قطعه‌ی مشترک ساخته شد:

- `Modal` — یک دیالوگ وسط‌چین با Escape، کلیک روی پس‌زمینه، قفل اسکرول، تله فوکوس
  و بازگشت فوکوس.
- `DetailDialog` — بدنه‌ی مشترک محصولات، مراحل، ویژگی‌ها، صنایع و نظرات.
- `ContactDialog` — کانال‌ها به‌صورت ردیف‌های بازشونده که شماره‌ها را نشان می‌دهند.

### مرحله ۴ — رفع باگ‌های پاپ‌آپ (`4555c3f`)

سه باگ واقعی که هر کدام درس خودشان را دارند:

- **پاپ‌آپ وسط صفحه نمی‌آمد.** داخل `Reveal` رندر می‌شد و `Reveal` یک `translate`
  دارد؛ هر ancestor با transform برای `position: fixed` یک containing block
  می‌سازد، پس پاپ‌آپ داخل کارت گیر می‌کرد. با `createPortal` روی `document.body` حل شد.
- **موقع بستن یک قاب خالی نشان می‌داد.** با زدن ✕ محتوا فوراً پاک می‌شد ولی انیمیشن
  خروج ۴۰۰ms طول می‌کشید. هوک `useRetained` محتوا را تا پایان انیمیشن نگه می‌دارد.
- **بار اول هیچ انیمیشنی دیده نمی‌شد.** انیمیشن ورود با تغییر state در React اجرا
  می‌شد که نیاز به یک فریم بین «مانت در حالت بسته» و «حالت باز» دارد. آن فریم گاهی
  نمی‌رسد. با `@starting-style` حالت شروع را به خود CSS سپردیم؛ دیگر هیچ
  زمان‌بندی جاوااسکریپتی در کار نیست.

### مرحله ۵ — MPA و صفحه برای هر آیتم نوبار (`eba571b`)

بررسی سایت چابک نشان داد صفحه دسته‌بندی‌شان ۱۱٬۲۶۰ پیکسل ارتفاع دارد و زیر گرید
محصولات یک مقاله ۱۴ هزار کاراکتری چسبانده‌اند. خریدار باید از رویش رد شود.

انتخاب ما: **متن بعد از دسته‌بندی‌ها**، نه قبلش. کسی که می‌داند چه می‌خواهد هرگز
مجبور نیست از آن رد شود.

هر آیتم نوبار صفحه‌ی واقعی خودش را گرفت. **نوبار به درخواست مشتری دست‌نخورده ماند** —
فقط مقصدها عوض شدند، نه برچسب‌ها و نه ترتیب.

### مرحله ۶ — ۱۳۵ صفحه محصول (`400c9f0`, `3e48402`)

هر تنوع محصول صفحه‌ی خودش را گرفت، در تب جدید. چیدمان: چهار عکس شناور سمت چپ در
نصف عرض، متن از راست دورشان می‌پیچد، و بعد از تمام شدن عکس‌ها تمام عرض را می‌گیرد.

**۱۴۷ صفحه با دست نگه‌داشتنی نیست**، پس `scripts/gen-pages.mjs` نوشته شد که همه را
از داده می‌سازد و به‌صورت `predev` و `prebuild` خودکار اجرا می‌شود.

### مرحله ۷ — بهینه‌سازی (`2aeab05`, `00eae9c`)

- **تفکیک داده هر خانواده.** هر صفحه محصول همه‌ی پنج کاتالوگ را باندل می‌کرد.
  **صرفه‌جویی حجم انتقال فقط ۳ KB بود، نه ۱۰–۱۲ KB که تخمین زده بودم** — چون gzip
  از قبل تکرار بین کاتالوگ‌های هم‌شکل را حذف کرده بود؛ من از روی حجم خام حساب کرده
  بودم. **برد واقعی جای دیگری بود:** هر صفحه حالا ۹ KB کاتالوگ را parse می‌کند
  به‌جای ۴۶ KB، و gzip به آن کمکی نمی‌کند.
- **preload فونت** با یک پلاگین کوچک build که اسم hash‌شده را از bundle می‌خواند.
- **سقف پرده لودینگ** از ۲۲۰۰ به ۸۰۰ میلی‌ثانیه.
- **`content-visibility` پیاده شد و برگردانده شد.** تخمین ارتفاع ۴۱۶ پیکسل بود ولی
  ارتفاع واقعی روی موبایل ۷۹۲ پیکسل درآمد — یعنی ۱۱۰۰ پیکسل پرش اسکرول‌بار. روی
  صفحه‌ای با ۱۰۷۳ نود سودش ناچیز و ریسکش واقعی بود.

### مرحله ۸ — سئو و ممیزی نهایی (`77be716` تا `d6190b0`)

- canonical، Open Graph، Twitter Card و JSON-LD روی هر ۱۴۷ صفحه. برای این سایت
  مهم‌تر از حد معمول است چون کل قیف به تلگرام ختم می‌شود و لینک بدون OG آنجا خالی
  نمایش داده می‌شود.
- sitemap با ۱۴۷ آدرس، robots.txt، صفحه ۴۰۴ (HTML ساده، بدون وابستگی به باندل).
- صفحه تماس با ما.
- بازسازی ساختار پوشه‌ها به `entries` / `pages` / `sections` / `layout` / `ui` /
  `hooks` / `context`.
- **باگی که ممیزی پیدا کرد:** فاوآیکون روی هر ۱۴۷ صفحه ۴۰۴ می‌داد. آدرسش
  `/favicon.svg` بود و Vite مسیرهای مطلق `public/` را بازنویسی نمی‌کند، پس به ریشه
  دامنه اشاره می‌کرد نه به `/Nylonium/`. روی سایت زنده تأیید و رفع شد.

---

## ۴. وضعیت فعلی

### صفحات

| نوع | تعداد | مسیر |
|---|---|---|
| صفحه اول | ۱ | `index.html` |
| تماس با ما | ۱ | `contact.html` |
| خانواده محصول | ۵ | `tagheei` `greenhouse` `shrink` `bags` `stretch` |
| همه محصولات هر خانواده | ۵ | `*-all.html` |
| تک محصول | ۱۳۵ | `product/<family>-<variant>.html` |
| **مجموع** | **۱۴۷** | |

### ساختار کد

```
src/
  entries/    نقطه ورود هر صفحه: main, product, contact
  pages/      قالب کل صفحه: Home, Family, AllProducts, Variant, Contact
  sections/   بخش‌های صفحه: Hero, Products, CategorySection, …
  layout/     Navbar, Footer, FloatingContact, PageLoader
  ui/         Modal, DetailDialog, ProductCard, VariantCard, Reveal, …
  hooks/      useTheme, useMediaQuery, useRetained
  context/    پاپ‌آپ تماس و کانتکستش
  data/       site.js (متن صفحه اول) و families/ (یک کاتالوگ برای هر خانواده)
  lib/        links.js
scripts/      gen-pages.mjs و قالب صفحه ۴۰۴
```

### دستورها

```bash
npm install
npm run dev      # http://localhost:5183
npm run build    # خروجی در dist/
npm run gen      # بازسازی شِل‌ها، sitemap و robots (خودکار قبل از dev و build)
```

### نتیجه ممیزی

| بررسی | نتیجه |
|---|---|
| لینک داخلی معیوب | ۰ از ۱۴۷ |
| عنوان تکراری | ۰ |
| توضیحات متا تکراری | ۰ |
| صفحه بدون `lang`/`dir` | ۰ |
| تصویر بدون `alt` | ۰ |
| دکمه بدون نام دسترس‌پذیر | ۰ |
| کنتراست متن (هر دو تم) | همه بالای AA |

---

## ۵. قواعدی که باید رعایت شوند

اینها تصمیم‌های عمدی‌اند، نه سلیقه. شکستن‌شان چیزی را خراب می‌کند:

1. **برای متن سبز از `--brand-ink` استفاده کن**، نه `brand-500/600`. آن دو کنتراست
   لازم را ندارند.
2. **`.rail` نباید `--rail-visible` بگیرد.** آن قانون unlayered است و بر utility
   ریسپانسیو غلبه می‌کند و همه بریک‌پوینت‌ها را روی یک کارت قفل می‌کند. مقدار
   پیش‌فرض داخل `calc()` است.
3. **ریل نباید padding افقی بگیرد.** scroll-snap به لبه‌ی border تراز می‌کند و هر
   پرش به اندازه padding خطا می‌خورد.
4. **پاپ‌آپ‌ها باید portal بمانند.** هر ancestor با transform آن‌ها را حبس می‌کند.
5. **فقط `opacity` و `transform` انیمیت شوند** تا کار روی ترد compositor بماند.
6. **مسیر فایل‌های `public/` را مطلق ننویس.** Vite بازنویسی‌شان نمی‌کند و روی
   `/Nylonium/` می‌شکنند.
7. **در `src/data/families/` پسوند `.js` را در import بنویس.** مولد صفحات در
   Node خام اجرا می‌شود و مسیرها را تحت‌اللفظی resolve می‌کند.

---

## ۶. داده‌های تستی که باید جایگزین شوند

| مورد | کجا |
|---|---|
| تصاویر محصولات | `gallery: []` در همه‌جا — الان SVG اختصاصی نشان داده می‌شود |
| ایمیل | `src/data/site.js` → `emailAddresses` |
| مشخصات فنی محصولات | `src/data/families/*.js` — بازه‌های عرض و ضخامت تخمینی‌اند |
| کاتالوگ ۱۳۵ تنوع | `src/data/families/*.js` — نمونه، بر اساس عرف بازار |
| نظرات مشتریان | `src/data/site.js` → `testimonials` — نام‌ها ساختگی |
| آمار | `src/data/site.js` → `stats` — اعداد نمونه |
| لینک تلگرام | `src/data/site.js` → `phoneNumbers` — اگر یوزرنیم داری بهتر است |

**آدرس و ساعت کاری به درخواست مشتری از کل سایت حذف شدند.**

---

## ۷. کارهای باقی‌مانده (هیچ‌کدام الزامی نیست)

| مورد | اثر اگر انجام نشود | زحمت |
|---|---|---|
| `og:image` (PNG ۱۲۰۰×۶۳۰) | لینک در تلگرام بدون عکس نمایش داده می‌شود | یک تصویر |
| متن اختصاصی هر تنوع، یا `noindex` روی آن‌ها | گوگل ۲۷ صفحه هر خانواده را محتوای تکراری می‌بیند | ۱۰ دقیقه (noindex) |
| پیش‌رندر ایستا (SSG) | هیچ. فقط لود کمی سریع‌تر می‌شد | نیم‌روز |

**اولویت پیشنهادی:** اول `og:image` (چون کل قیف فروش به تلگرام می‌رود)، بعد
`noindex` روی صفحات تنوع (ارزان‌ترین راه حل محتوای تکراری، بدون نوشتن یک کلمه).

برای SSG لازم است کدهای مرورگرمحور اول محافظت شوند: `links.js`، `useTheme`،
`useMediaQuery`، `PageLoader` و portal مربوط به `Modal`.

---

## ۸. نکته‌ای درباره‌ی این گزارش

بعضی چیزها در این پروژه **قابل تأیید چشمی نبودند**، چون پنل مرورگر ابزار توسعه
فریم رندر نمی‌کرد (`document.visibilityState` برابر `hidden` و
`document.timeline.currentTime` روی صفر ثابت). این موارد شامل انیمیشن پاپ‌آپ،
حرکت اسکرول ریل و انتقال‌های CSS بود.

آنچه در این موارد انجام شد: به‌جای ادعای «تست شد»، ساختار زیرین اندازه‌گیری شد —
مثلاً وجود دو `CSSTransition` با مدت ۶۲۰ms و وضعیت `running`، یا صحت حساب گام
ریل (stride ۲۴۲px، سه کارت در نما، کلمپ روی آخرین کارت). هرجا چنین محدودیتی
وجود داشت، صریح گفته شد.

---

*آخرین به‌روزرسانی: ۱۹ شهریور ۱۴۰۵ — کامیت `d6190b0`*
